spring.application.name=secure-auth

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/secure_auth_db
spring.datasource.username=root
spring.datasource.password=anik@9798

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080

# JWT Configuration
jwt.secret=MySuperSecretKeyForJWTAuthenticationProject2026SecureAuth123456
jwt.expiration=86400000 
# The JWT expiration time is set to 86400000 milliseconds, which is equivalent to 24 hours.